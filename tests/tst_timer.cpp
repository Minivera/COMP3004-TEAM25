#include "tst_timer.h"

void TimerTest::A()
{
    /*// Arrange
    Timer window;

    // Act
    window.show();
    QSignalSpy spy(window.timer200, SIGNAL(timeout()));

    // Assert
    QCOMPARE(spy.isValid(), true);
    QVERIFY(spy.wait());*/
}
